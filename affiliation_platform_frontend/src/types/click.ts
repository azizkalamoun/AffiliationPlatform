export type Click = {
  id?: string;
  urlId: string;
  affiliateId: string;
  createdAt?: Date;
};

export type createClickProps = {
  id?: string;
  urlId: string;
  affiliateId: string;
  createdAt?: Date;
};
